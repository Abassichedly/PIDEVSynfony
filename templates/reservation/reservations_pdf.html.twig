<!DOCTYPE html>
<html>
<head>
    <title>Reservations List</title>
    <style>
        body { font-family: Arial, sans-serif; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        th { background-color: #34b7a7; color: white; }
        h1 { color: #34b7a7; }
    </style>
</head>
<body>
    <h1>Reservations for {{ sportSpace.name }}</h1>
    <p>Generated on: {{ date|date('Y-m-d H:i:s') }}</p>
    <p>Sport Space Location: {{ sportSpace.location }}</p> <!-- Changed from address to location -->
    
    <table>
        <thead>
            <tr>
                <th>Date</th>
                <th>Time</th>
                <th>Duration</th>
            </tr>
        </thead>
        <tbody>
            {% for reservation in reservations %}
                <tr>
                    <td>{{ reservation.date|date('Y-m-d') }}</td>
                    <td>{{ reservation.time|date('H:i') }}</td>
                    <td>{{ reservation.duration }} hours</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>